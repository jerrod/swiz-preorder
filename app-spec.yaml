name: swiz-preorder
region: nyc
static_sites:
- name: swiz-preorder
  source_dir: public
  output_dir: /
  index_document: index.html
  error_document: index.html
  routes:
  - path: /
  git:
    repo_clone_url: https://github.com/dischord/swiz-preorder.dischord.com.git
    branch: main
domains:
- domain: swiz-preorder.dischord.com
  type: PRIMARY
